## SQL Basics Covered
- Database creation
- Table creation
- Data types
- Constraints
- Insert & Select queries

📘 01_Basics – SQL Fundamentals

This folder contains basic SQL concepts required for beginners and interview preparation.
All examples are written in MySQL.

1️⃣ Database Creation

Database is created to store tables and data.


2️⃣ Table Creation

Tables store data in rows and columns.

users table is created with proper structure and constraints.


3️⃣ Data Types

Used to define the type of data stored in a column.

INT → numeric values

VARCHAR → text data

DECIMAL → salary / price values

DATE → date values

TIMESTAMP → date & time

ENUM → fixed set of values

4️⃣ Constraints

Used to maintain data integrity.

PRIMARY KEY → unique identification

AUTO_INCREMENT → automatic id generation

NOT NULL → value required

UNIQUE → no duplicate values

DEFAULT → automatic default value

5️⃣ Insert Queries

Used to add records into the table.

Supports inserting multiple rows in one query.

INSERT INTO users (...) VALUES (...);

6️⃣ Select Queries

Used to retrieve data from table.

Filtering, sorting and limiting results.

SELECT * FROM users;
SELECT name, salary FROM users WHERE salary > 30000;

✅ Learning Outcome

Understood database & table creation

Learned data types and constraints

Practiced insert and select queries

🛠 Tools Used

MySQL | MySQL Workbench | VS Code | GitHub

⭐ Part of my SQL learning journey for placements and interviews.
