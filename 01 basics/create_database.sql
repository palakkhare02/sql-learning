-- This command creates a database
-- It will throw an error if the database already exists
-- CREATE DATABASE startersql;

-- Safe way to create a database
-- This will not throw an error if the database already exists
CREATE DATABASE IF NOT EXISTS startersql;

-- Select the database
USE startersql;
